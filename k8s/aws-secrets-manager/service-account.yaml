apiVersion: v1
kind: ServiceAccount
metadata:
  name: atlas-alerts-sa
  namespace: atlas-alerts
  annotations:
    # This annotation links the K8s service account to an IAM role
    # Replace with your actual IAM role ARN
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/atlas-alerts-secrets-role
